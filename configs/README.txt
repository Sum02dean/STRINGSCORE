To load environment: conda env create --name envname --file=env_config/environment.yml  
Don't foget to specify the --name as [envname] of your choice.

1. Go to src dir
2. Activate the python env
3. Copy the data: cp /mnt/mnemo1/sum02dean/dean_mnt/projects/STRINGSCORE/src [PATH] 
2. Configure run_xgboost_model.sh file with desired parameters
3. Run 'bash run_xgboost.sh'
4. Done
